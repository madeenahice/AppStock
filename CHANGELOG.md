# บันทึกการแก้ไข

## 8 กันยายน 2026

- Med Stock, IV Fluid, CPR Box, Emer medicine, Emer cart Adult และ Emer cart PED เลือกหมายเหตุ รอเบิก/ตามไม่ได้ และ Home แสดงกลุ่มตามไม่ได้

- Equipment เลือกหมายเหตุจำนวนไม่ครบเฉพาะ ถูกยืม/ส่งซ่อม และเพิ่มกลุ่มถูกยืมใน Home

- ดึงต้นฉบับข้อมูลจาก Google Sheet “รายการเช็คของ RN” ครบ 7 หมวด รวมแถวชื่อและจำนวนเหมือนกันตามคำสั่งผู้ใช้ เหลือ 179 รายการจาก 204 แถว
- จัด CPR Box ตาม C:D และ Emer medicine ตาม A:B ซึ่งต่างจากการจัดหมวดใน Excel เดิม
- กำหนด Thermo trace peripheral ใน Emer cart PED เป็น 3 ตามที่ผู้ใช้แจ้ง
- เพิ่มไฟล์ข้อมูลต้นฉบับและสคริปต์สร้าง seed พร้อมย้ายข้อมูลเก่าโดยสำรองก่อนและรักษาข้อมูลตรวจของรายการที่จับคู่ได้

- เพิ่ม dropdown หมายเหตุ รอเบิก/หาย/ส่งซ่อม เฉพาะรายการที่จำนวนไม่ครบ และสรุปแยกกลุ่มใน Home พร้อมหน่วยและจำนวนที่ขาด
- ล้างหมายเหตุปัจจุบันเมื่อเติมจำนวนครบ เก็บหมายเหตุไว้ในประวัติการตรวจและส่งไป Google Sheets/Telegram

- เรียงเมนู Home, Med Stock, IV Fluid, Equipment, CPR Box, Emer medicine, Emer cart Adult, Emer cart PED, Dashboard ทั้ง desktop และมือถือ โดยแยก Home กับ Dashboard เป็นคนละเมนู

- เปลี่ยนชื่อหน่วยเป็น Emer medicine, Emer cart Adult, Emer cart PED พร้อมรองรับชื่อเดิมในข้อมูลที่บันทึกและนำเข้า
- เปลี่ยน Home เป็น Dashboard และเพิ่มหน้าสต๊อก Med Stock, IV Fluid, Equipment
- เพิ่มวันที่ตรวจ เวรดึก/เช้า/บ่าย และ dropdown จำชื่อผู้ตรวจ พร้อมบันทึกลงประวัติและส่งในข้อความ Telegram
- กำหนด Google Sheet ปลายทางและแยกแท็บหน่วย/สรุปตามปีงบประมาณ พ.ศ. ต.ค.–ก.ย. จากวันที่ตรวจ
- ย้าย cloud snapshot ไปยังแท็บ App State และป้องกันคำขอบันทึกตรวจซ้ำด้วย Log ID
- เพิ่มการตรวจใบรับบันทึกจากเซิร์ฟเวอร์เมื่อส่งแบบ no-cors และชุดทดสอบการย้ายข้อมูล/ปีงบประมาณ
- ต้อง deploy Apps Script เวอร์ชันใหม่ก่อนใช้การบันทึกแบบใหม่

## 6 มิถุนายน 2026

### ปรับ UX/UI
- ปรับขนาดหน้าให้เหมาะกับโทรศัพท์มากขึ้น
- ลดขนาดหัวข้อ, subtitle, การ์ด summary, ปุ่ม, ช่องกรอก และตารางบนหน้าจอเล็ก
- จัด dashboard บนมือถือเป็น 2 คอลัมน์ และเปลี่ยนเป็น 1 คอลัมน์เฉพาะจอแคบมาก
- ทำ sidebar บนมือถือเป็นแถบด้านบนแบบ sticky พร้อมเมนูที่เลื่อนแนวนอนได้
- เปลี่ยนเมนูเลือกหน่วยบนมือถือจากปุ่มหลายปุ่มเป็น dropdown เพื่อลดพื้นที่และกดง่ายขึ้น
- ซ่อนปุ่มพับ sidebar บนมือถือเพื่อลดความรก
- ปรับ toolbar บนมือถือให้ช่องค้นหา, sort, filter เต็มบรรทัด และปุ่ม action เรียงเป็นช่องเท่ากัน
- ลดความกว้างขั้นต่ำของตารางบนมือถือ และปรับปุ่มเพิ่ม/ลดจำนวนให้แตะง่ายขึ้น
- เปลี่ยนตารางบนมือถือเป็น card layout ต่อรายการ เพื่อไม่ให้คอลัมน์ถูกตัดหรืออ่านยาก
- ซ่อน `สถานะ` และ `ตรวจล่าสุด` ในรายการแต่ละแถวบนมือถือ โดยแสดง `ตรวจล่าสุด` รวมไว้ด้านบน section แค่จุดเดียว
- ย้ายแถบลงชื่อผู้ตรวจสอบไปไว้ด้านล่างสุดของตารางบนมือถือ
- ปรับ modal ให้มีระยะขอบและ padding เหมาะกับจอโทรศัพท์

### เพิ่ม
- เพิ่มช่องตั้งค่า `Google Sheet Web App URL` ในหน้าต่างตั้งค่า
- เพิ่มการส่งข้อมูลทุกครั้งที่กด `บันทึกการตรวจ` ไปยัง Google Sheet ผ่าน Google Apps Script Web App
- เพิ่มไฟล์ตัวอย่าง [google-sheets-web-app.gs](./google-sheets-web-app.gs) สำหรับนำไปวางใน Apps Script ของ Google Sheet
- ฝัง Google Sheet Web App URL เริ่มต้นไว้ในระบบ เพื่อให้ส่งข้อมูลได้ทันทีหากยังไม่ได้ตั้งค่า URL เอง
- เพิ่ม version query ให้ `styles.css`, `seed-data.js`, และ `app.js` เพื่อบังคับให้มือถือโหลดไฟล์ล่าสุดแทนไฟล์ cache เก่า
- เพิ่ม cloud sync สำหรับข้อมูล stock และประวัติการเช็คผ่าน Google Apps Script เพื่อให้ PC และมือถือใช้ข้อมูลกลางชุดเดียวกัน
- อัปเดต Apps Script ให้รองรับ `saveState` และ `loadState` ผ่าน JSONP สำหรับโหลดข้อมูลข้ามเครื่อง
- เปลี่ยน Google Sheet Web App URL เริ่มต้นเป็น URL `/exec` ใหม่ และ migrate URL เก่าที่เป็น `googleusercontent` อัตโนมัติ
- เปลี่ยน Google Sheet Web App URL เริ่มต้นเป็น endpoint ใหม่ และ migrate endpoint เก่าไปยัง URL ล่าสุดอัตโนมัติ
- เพิ่ม popup/toast ข้อความ `บันทึกสำเร็จ` หลังบันทึกการตรวจ, บันทึกรายการ, และบันทึกการตั้งค่า
- ปรับ Google Apps Script ให้บันทึกรายการตรวจลง sheet แยกตามหน่วยเท่านั้น: `CPR Box`, `ยารถ Emer`, `รถ EMER Adult`, `รถ EMER PED`
- เพิ่ม action `sendTelegram` ใน Google Apps Script และปรับเว็บให้ส่ง Telegram ผ่าน Apps Script เพื่อลดปัญหา browser/mobile ส่งไม่ออก
- ปรับ popup หลังบันทึกตรวจให้บอกสถานะ Telegram ชัดเจน
- แก้ปุ่ม `ยกเลิก` และปุ่มปิดในหน้าต่างเพิ่ม/แก้ไขรายการ ให้ปิดหน้าต่างโดยไม่บันทึกข้อมูล
- ฝังค่า Telegram Bot Token และ Chat ID ไว้ฝั่ง Google Apps Script เพื่อให้ทุกเครื่องส่งแจ้งเตือนผ่าน endpoint กลางได้โดยไม่ต้องตั้งค่า token ซ้ำ
- ปรับหน้าเว็บให้ส่ง Telegram ผ่าน Apps Script ได้แม้ช่อง Bot Token และ Chat ID ในหน้าเว็บจะว่าง
- เปลี่ยน Google Sheet Web App URL เริ่มต้นเป็น deployment ใหม่ และเพิ่ม URL เก่าในรายการ migrate อัตโนมัติ
- ปรับ Apps Script ให้ไม่สร้างหรือบันทึกรายละเอียดลง `Inspection Items` รวมแล้ว โดยรายละเอียดจะลงเฉพาะชีทหน่วย `CPR Box`, `ยารถ Emer`, `รถ EMER Adult`, `รถ EMER PED`
- ปรับ Telegram ตอนบันทึกการตรวจให้ส่งรายการทั้งหมดของหน่วยที่ตรวจ พร้อมจำนวนที่นับได้/ต้องมี วันหมดอายุ และสถานะของแต่ละรายการ
- เปลี่ยน Google Sheet Web App URL เริ่มต้นเป็น deployment ล่าสุด และเพิ่ม deployment ก่อนหน้าในรายการ migrate อัตโนมัติ
- เพิ่มฟังก์ชัน `testTelegram()` ใน Apps Script สำหรับกด Run เพื่อ authorize สิทธิ์ `UrlFetchApp.fetch` และทดสอบส่ง Telegram
- ปรับ popup หลังบันทึกการตรวจให้ขึ้นข้อความ `บันทึกสำเร็จ` ทันทีหลังบันทึกในระบบ ก่อนส่ง Google Sheet/Telegram ต่อ
- เปลี่ยนปลายทาง Telegram จากแชทส่วนตัวเป็นกลุ่ม `Check EmergencyCart`
- เปลี่ยน Google Sheet Web App URL เริ่มต้นเป็น deployment ใหม่ล่าสุด และเพิ่ม deployment ก่อนหน้าในรายการ migrate อัตโนมัติ

## 5 มิถุนายน 2026

### เพิ่ม
- เพิ่มตารางรายการสต๊อกแบบมีหัวตาราง พร้อมตัวกรองสถานะและการเรียงรายการ
- เพิ่ม sidebar สำหรับเมนู `Home`, `CPR Box`, `ยารถ Emer`, `รถ EMER adult`, `รถ EMER ped`
- เพิ่ม sidebar แบบพับ/ขยายได้ และจำสถานะพับไว้ในเครื่อง
- เพิ่มสัญลักษณ์สำหรับ sidebar ตอนพับ เช่น Home, CPR, Rx, Adult, Ped และตั้งค่า Telegram
- เพิ่มเมนู `ตั้งค่า Telegram` ใน sidebar แทนปุ่มเฟืองด้านบน
- เพิ่มหน้า Home แบบภาพรวม มีเฉพาะการ์ดสรุป, แจ้งเตือน, และประวัติการเช็คสต๊อก
- เพิ่มการเปลี่ยนหัวข้อหน้าตามเมนูที่เลือก เช่น `CPR Box`, `ยารถ Emer`
- เพิ่มการล็อคหัวตารางให้ค้างอยู่ด้านบนเวลาเลื่อนในตาราง
- เพิ่มการบันทึก changelog ไฟล์นี้ไว้ใช้ดูย้อนหลัง

### แก้ไข Layout
- เปลี่ยนรายการจาก card list เป็น table ที่จัดการ stock ได้ในแถวเดียว
- ย้าย `CPR Box`, `ยารถ Emer`, `รถ EMER adult`, `รถ EMER ped` จาก tab ด้านบนไปไว้ใน sidebar
- ล็อค sidebar ไม่ให้เลื่อนไปกับหน้าเพจบน desktop
- ทำให้แต่ละบล็อก scroll เฉพาะภายในตัวเอง แทนการ scroll ทั้งหน้า
- ย้ายแถบค้นหา, sort/filter, และปุ่ม action ลงมาอยู่ติดกับตาราง
- จัด `แจ้งเตือน` และ `ประวัติการเช็คสต๊อก` เป็น 2 คอลัมน์ในหน้า Home
- ซ่อนตารางในหน้า Home
- ซ่อน `แจ้งเตือน` และ `ประวัติการเช็คสต๊อก` ในเมนูย่อย
- จัดแถว `ผู้ตรวจสอบ`, ช่องกรอกชื่อ, สถานะตรวจล่าสุด, และปุ่ม `บันทึกการตรวจ` ให้อยู่บรรทัดเดียวกัน
- ย้ายข้อความ `ยังไม่มีบันทึกการตรวจ` มาไว้ระหว่างช่องกรอกชื่อกับปุ่มบันทึก
- เอากล่องล่าง `ระบบสต๊อก / Medication & Supply` ออกจาก sidebar

### ปรับ UX/UI
- ลดขนาดภาพรวมของหน้า เช่น sidebar, หัวข้อ, การ์ด, toolbar, ตาราง เพื่อไม่ให้ล้นจอ
- เปลี่ยนปุ่ม action เป็นปุ่มสัญลักษณ์ เช่น `XLS`, `{}`, `↺`, `⇣`, `⇡`, `+`
- จัดช่องค้นหา, sort/filter ชิดซ้าย และปุ่ม action ชิดขวา
- ขยายกล่อง `แจ้งเตือน` และ `ประวัติการเช็คสต๊อก` ในหน้า Home ให้เต็มพื้นที่มากขึ้น
- ลดขนาดกล่อง `แจ้งเตือน` และ `ประวัติการเช็คสต๊อก` ในช่วงที่ต้องการให้กะทัดรัด ก่อนปรับเป็นพื้นที่ Home
- ปรับสีสถานะให้แยกความหมายชัดเจน:
  - `ใกล้หมดอายุ` ใช้โทนเหลือง
  - `หมดอายุแล้ว` ใช้โทนแดง
  - `สต๊อกต่ำกว่าเกณฑ์` ใช้โทนม่วง
- เอาสีเขียวออกจากสถานะที่ต้องแก้ไข เพื่อไม่ให้ดูเหมือนผ่านหรือไม่มีปัญหา
- เพิ่มสี active ให้เมนู sidebar ที่เลือกอยู่
- ปรับ alignment ตารางรายการให้คอลัมน์ส่วนใหญ่และคอลัมน์จัดการอยู่กึ่งกลาง โดยคอลัมน์ `รายการ` ชิดซ้าย
- เปลี่ยนการ์ด `รายการทั้งหมด` เป็นสีเขียว
- เปลี่ยนสัญลักษณ์แบรนด์ใน sidebar จาก `✺` เป็น `⚕`
- ปรับสัญลักษณ์เมนูหน่วยใน sidebar เป็น `❤`, `𓊔`, `⚕`, `⚕` และจัดให้อยู่กึ่งกลางปุ่มตอนพับ

### พฤติกรรมข้อมูล
- หน้า Home แสดงการ์ด summary เป็นภาพรวมทุกหน่วย
- เมนูย่อยแสดงการ์ด summary เฉพาะหน่วยที่เลือก
- ปุ่ม Home จะกลับไปภาพรวม, เคลียร์การค้นหา, sort/filter และเลื่อนขึ้นบนหน้า
- ตารางและเครื่องมือจัดการรายการแสดงเฉพาะในเมนูย่อย
